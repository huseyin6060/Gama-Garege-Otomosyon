<UserControl x:Class="GamaGarage.Sayfalar.GrafiklerSayfasi"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="1200" d:DesignWidth="1920">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <UniformGrid Columns="2" Margin="30">

                <materialDesign:Card Background="#25FFFFFF" UniformCornerRadius="25" Margin="15" Padding="20">
                    <Grid>
                        <TextBlock Text="MEKAN DOLULUK" HorizontalAlignment="Center" VerticalAlignment="Top" Foreground="Black" FontWeight="Bold"/>
                        <lvc:PieChart Height="320" x:Name="ChartMekan" LegendLocation="Bottom" Margin="0,30,0,0" InnerRadius="0" Foreground="Black"/>
                    </Grid>
                </materialDesign:Card>

                <materialDesign:Card Background="#25FFFFFF" UniformCornerRadius="25" Margin="15" Padding="20">
                    <Grid>
                        <TextBlock Text="MÜŞTERİ PROFİLİ (YENİ/ESKİ)" HorizontalAlignment="Center" VerticalAlignment="Top" Foreground="#FF5722" FontWeight="Bold"/>
                        <lvc:PieChart Height="320" x:Name="ChartMusteri" Margin="0,30,0,0" InnerRadius="70" LegendLocation="Bottom" Foreground="Black"/>
                    </Grid>
                </materialDesign:Card>

                <materialDesign:Card Background="#25FFFFFF" UniformCornerRadius="25" Margin="15" Padding="20">
                    <Grid>
                        <TextBlock Text="KASA DURUMU" HorizontalAlignment="Center" VerticalAlignment="Top" Foreground="#0288D1" FontWeight="Bold"/>
                        <lvc:CartesianChart Height="320" x:Name="ChartGelirGider" Margin="0,30,0,0">
                            <lvc:CartesianChart.Series>
                                <lvc:ColumnSeries Title="Bakiye" Values="{Binding GelirGiderDegerleri}" DataLabels="True" Foreground="Black" MaxColumnWidth="50">
                                    <lvc:ColumnSeries.Fill>
                                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                            <GradientStop Color="#2ECC71" Offset="0"/>
                                            <GradientStop Color="#27AE60" Offset="1"/>
                                        </LinearGradientBrush>
                                    </lvc:ColumnSeries.Fill>
                                </lvc:ColumnSeries>
                            </lvc:CartesianChart.Series>
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="GELİR,GİDER" Foreground="Black" FontWeight="Bold"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Foreground="Black" FontWeight="Bold"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </materialDesign:Card>

                <materialDesign:Card Background="#25FFFFFF" UniformCornerRadius="25" Margin="15" Padding="20">
                    <Grid>
                        <TextBlock Text="GÜNLÜK SERVİS TRAFİĞİ" HorizontalAlignment="Center" VerticalAlignment="Top" Foreground="#FBC02D" FontWeight="Bold"/>
                        <lvc:CartesianChart Height="320" x:Name="ChartServis" Margin="0,30,0,0">
                            <lvc:CartesianChart.Series>
                                <lvc:LineSeries Title="Araç" Values="{Binding ServisDegerleri}" Stroke="#FBC02D" Fill="Transparent" PointGeometrySize="10" DataLabels="True" Foreground="Black"/>
                            </lvc:CartesianChart.Series>
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding Gunler}" Foreground="Black" FontWeight="Bold"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Foreground="Black" FontWeight="Bold"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </materialDesign:Card>

                <materialDesign:Card Background="#25FFFFFF" UniformCornerRadius="25" Margin="15" Padding="20">
                    <Grid>
                        <TextBlock Text="KRİTİK STOK ANALİZİ" HorizontalAlignment="Center" VerticalAlignment="Top" Foreground="#AFB42B" FontWeight="Bold"/>
                        <lvc:CartesianChart Height="320" x:Name="ChartStok" Margin="0,30,0,0">
                            <lvc:CartesianChart.Series>
                                <lvc:RowSeries Title="Adet" Values="{Binding StokDegerleri}" Fill="#F1C40F" DataLabels="True" Foreground="Black"/>
                            </lvc:CartesianChart.Series>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Labels="{Binding StokIsimleri}" Foreground="Black" FontWeight="Bold"/>
                            </lvc:CartesianChart.AxisY>
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Foreground="Black" FontWeight="Bold"/>
                            </lvc:CartesianChart.AxisX>
                        </lvc:CartesianChart>
                    </Grid>
                </materialDesign:Card>

                <materialDesign:Card Background="#25FFFFFF" UniformCornerRadius="25" Margin="15" Padding="20">
                    <Grid>
                        <TextBlock Text="PERSONEL MAAŞ DAĞILIMI" HorizontalAlignment="Center" VerticalAlignment="Top" Foreground="#8E24AA" FontWeight="Bold"/>
                        <lvc:CartesianChart Height="320" x:Name="ChartMaaslar" Margin="0,30,0,0">
                            <lvc:CartesianChart.Series>
                                <lvc:LineSeries Title="Maaş" Values="{Binding MaasDegerleri}" Stroke="#CE93D8" PointGeometrySize="5" DataLabels="True" Foreground="Black">
                                    <lvc:LineSeries.Fill>
                                        <SolidColorBrush Color="#CE93D8" Opacity="0.3"/>
                                    </lvc:LineSeries.Fill>
                                </lvc:LineSeries>
                            </lvc:CartesianChart.Series>
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding PersonelIsimleri}" Foreground="Black" FontWeight="Bold"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Foreground="Black" FontWeight="Bold"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </materialDesign:Card>

            </UniformGrid>
        </ScrollViewer>
    </Grid>
</UserControl>